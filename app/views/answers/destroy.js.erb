<% if !@answer.persisted? %>
  $('.best-answer').html('<%= j render 'answers/best_answer', resource: @question.best_answer %>')
  $('.other-answers').html('<%= j render @question.answers.where.not(id: @question.best_answer_id) %>')
<% end %>
